On a fresh machine (with only `git` installed):

```
git clone git@github.com:antila/dotfiles.git ~/.dotfiles
~/.dotfiles/install.sh
```

`install.sh` takes care of everything else: `stow`, `curl`, `fnm`, Node.js, `zx`, `npm install`, and running `install.mjs`.
